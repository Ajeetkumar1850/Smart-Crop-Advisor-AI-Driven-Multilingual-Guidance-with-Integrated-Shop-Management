<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Add Product | AGRO</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      max-width: 600px;
      margin: 0 auto;
      padding: 2rem;
      background-color: #f8fbf3;
      color: #333;
    }
    h3 {
      color: #2d7a4c;
      margin-bottom: 1.5rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid #c8e6c9;
    }
    form {
      background: white;
      padding: 1.5rem;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    label {
      display: block;
      margin: 1rem 0 0.5rem;
      color: #2d7a4c;
      font-weight: 500;
    }
    input {
      width: 100%;
      padding: 0.75rem;
      border: 1px solid #ddd;
      border-radius: 4px;
      font-size: 1rem;
      transition: all 0.2s;
    }
    input:focus {
      outline: none;
      border-color: #2d7a4c;
      box-shadow: 0 0 0 2px rgba(45, 122, 76, 0.2);
    }
    button, .btn {
      background-color: #2d7a4c;
      color: white;
      border: none;
      padding: 0.75rem 1.5rem;
      border-radius: 4px;
      cursor: pointer;
      font-size: 1rem;
      margin-top: 1.5rem;
      transition: all 0.2s;
      display: inline-block;
      text-decoration: none;
    }
    button:hover, .btn:hover {
      background-color: #1e5631;
      transform: translateY(-2px);
    }
    .btn-cancel {
      background-color: #f5f5f5;
      color: #666;
      margin-right: 1rem;
    }
    .btn-cancel:hover {
      background-color: #e0e0e0;
      color: #333;
    }
    .action-buttons {
      display: flex;
      gap: 1rem;
      margin-top: 2rem;
    }
  </style>
</head>
<body>
  <form method="POST" action="/prod">
    <h3>➕ Add New Product</h3>

    <label for="name">Product Name:</label>
    <input type="text" id="name" name="product[name]" required placeholder="Enter product name"/>
    
    <label for="price">Price (₹):</label>
    <input type="number" id="price" name="product[price]" required placeholder="Enter price" min="0" step="0.01"/>
    
    <label for="description">Description:</label>
    <input type="text" id="description" name="product[description]" required placeholder="Enter product description"/>
    
    <label for="image">Image URL:</label>
    <input type="url" id="image" name="product[image]" required placeholder="https://example.com/image.jpg"/>

    <button type="submit">Add Product</button>
  </form>

  <div class="action-buttons">
    <a href="/prod" class="btn btn-cancel">Cancel</a>
    <a href="/prod" class="btn">Back to Products</a>
  </div>
</body>
</html>