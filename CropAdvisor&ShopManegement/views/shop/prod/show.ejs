<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Product Details</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #c6d338;
      margin: 0;
      padding: 2rem;
      display: flex;
      justify-content: center;
    }

    .product-card {
      background-color: #92d2d5;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      padding: 2rem;
      max-width: 500px;
      width: 100%;
    }

    h3 {
      text-align: center;
      color: #2e7d32;
      margin-bottom: 1.5rem;
    }

    ul {
      list-style: none;
      padding: 0;
      margin: 0 0 1.5rem;
    }

    ul li {
      margin-bottom: 1rem;
      font-size: 1.1rem;
    }

    img {
      display: block;
      border-radius: 8px;
      margin: 0 auto;
    }

    .btn-group {
      display: flex;
      justify-content: space-between;
      gap: 1rem;
    }

    button, a.button-link {
      padding: 0.6rem 1.2rem;
      font-size: 1rem;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background 0.3s ease;
      text-decoration: none;
      text-align: center;
      display: inline-block;
    }

    button[type="submit"] {
      background-color: #e53935;
      color: white;
    }

    button[type="submit"]:hover {
      background-color: #c62828;
    }

    a.button-link.edit {
      background-color: #0288d1;
      color: white;
    }

    a.button-link.edit:hover {
      background-color: #0277bd;
    }

    a.button-link.back {
      background-color: #9e9e9e;
      color: white;
    }

    a.button-link.back:hover {
      background-color: #757575;
    }
  </style>
</head>
<body>
  <div class="product-card">
    <h3>Product Details</h3>
    <ul>
      <li><strong>Name:</strong> <%= product.name %></li>
      <li>
        <img src="<%= product.image.url %>" alt="Product Image" width="200" height="200">
      </li>
      <li><strong>Price:</strong> ₹<%= product.price %></li>
      <li><strong>Description:</strong> <%= product.description %></li>
    </ul>
    
    <form method="POST" action="/prod/<%= product._id %>?_method=DELETE">
      <div class="btn-group">
        <button type="submit">Delete</button>
        <a href="/prod/<%= product._id %>/edit" class="button-link edit">Edit</a>
        <a href="/prod" class="button-link back">Back</a>
      </div>
    </form>
  </div>
</body>
</html>
