<!DOCTYPE html>
<html>
<head>
    <title>Edit Worker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { padding: 20px; background-color: #f8f9fa; }
        .edit-container { max-width: 600px; margin: 30px auto; background: white; padding: 30px; border-radius: 8px; box-shadow: 0 0 15px rgba(0,0,0,0.1); }
        h1 { margin-bottom: 25px; color: #343a40; border-bottom: 2px solid #eee; padding-bottom: 10px; }
        .form-actions { margin-top: 20px; display: flex; justify-content: space-between; }
    </style>
</head>
<body>
    <div class="container">
        <div class="edit-container">
            <h1>Edit <%= worker.name %></h1>
            
            <form action="/worker/<%= worker._id %>?_method=PUT" method="POST">
                <div class="mb-3">
                    <label class="form-label">Name:</label>
                    <input type="text" class="form-control" name="worker[name]" value="<%= worker.name %>" required>
                </div>
                
                <div class="mb-3">
                    <label class="form-label">Role/Position:</label>
                    <input type="text" class="form-control" name="worker[role]" value="<%= worker.role %>" required>
                </div>
                
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label">Age:</label>
                        <input type="number" class="form-control" name="worker[age]" value="<%= worker.age %>">
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Salary:</label>
                        <input type="number" class="form-control" name="worker[salary]" value="<%= worker.salary %>">
                    </div>
                </div>
                
                <div class="mb-3">
                    <label class="form-label">Contact:</label>
                    <input type="text" class="form-control" name="worker[contact]" value="<%= worker.contact %>">
                </div>
                
                <div class="form-actions">
                    <a href="/worker/<%= worker._id %>" class="btn btn-outline-secondary">Cancel</a>
                    <button type="submit" class="btn btn-primary">Update Worker</button>
                </div>
            </form>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>